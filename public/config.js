// Aponta o cliente para o host da API/WebSocket (backend no Railway).
// Em dev local (mesmo processo servindo tudo), deixe undefined — net.js cai
// em location.host automaticamente.
window.SF_API_HOST = 'sunfall-arena-api-production.up.railway.app';
