# ============================================================
# SUNFALL ARENA — variáveis de ambiente do servidor
# Em produção, configure via secrets do Fly:
#   fly secrets set DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/..."
# ============================================================

# Postgres (Railway)
DATABASE_URL=

# Assinatura dos tokens de sessão (JWT)
JWT_SECRET=

# Webhook do Discord que recebe sugestões e reportes de bug da
# Central da Comunidade. Sem ele o jogo funciona normalmente —
# os envios ficam apenas registrados no banco.
DISCORD_WEBHOOK_URL=

# Webhook do canal de ANÚNCIOS: toda release nova de patch note é
# anunciada automaticamente (resumo + link pro jogo) no boot do
# servidor após o deploy. Sem ele, nenhum anúncio é enviado.
DISCORD_PATCHNOTES_WEBHOOK_URL=

# URL pública do jogo usada nos links dos anúncios
# (padrão: https://sunfall-arena.vercel.app)
GAME_URL=

# Origens permitidas no CORS (separadas por vírgula)
CORS_ORIGIN=http://localhost:3000
